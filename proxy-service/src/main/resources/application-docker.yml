spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/mcp_gateway
    driver-class-name: org.postgresql.Driver
    username: mcp
    password: mcp
  data:
    redis:
      host: redis
      port: 6379
